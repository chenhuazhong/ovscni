# permissions for end users to view ips.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ip-viewer-role
rules:
- apiGroups:
  - ovscni.github.com
  resources:
  - ips
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ovscni.github.com
  resources:
  - ips/status
  verbs:
  - get
